header

.hero.home

.pickers(ng-click="pickWhereWhen()")
  .when
    img.icon(src='/img/caravan_calendar.svg')
    .txt {{ start_date }} - {{ end_date }}
  .where
    img.icon(src='/img/caravan_world.svg')
    .txt {{ location }}

.trips
  trip(ng-repeat="trip in trips")
